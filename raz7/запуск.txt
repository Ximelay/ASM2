Правильная последовательность сборки в командной строке:

    Сначала компилируем ASM в объектный файл:

`ml64 /c raz7.asm`

Затем компилируем C++ и линкуем с объектным файлом:

cl /EHsc main.cpp raz7.obj